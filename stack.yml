services:
  proxy:
    image: eldaratech/swarm-rbac-proxy:latest
    deploy:
      placement:
        constraints: [node.role == manager]
    ports:
      - "2376:2376"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PROXY_LISTEN=:2376
